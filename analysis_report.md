# BTCUSDT 强化学习交易系统 — 训练报告

> 生成时间: 2026-06-08  
> 项目: ReinforcementTrading  
> 框架: Stable-Baselines3 PPO + Gym/Gymnasium  
> 模型: model_eurusd_best.zip

---

## 1. 数据迁移：EURUSD → BTCUSDT

### 1.1 数据源变更

| 项目 | 旧 (EURUSD) | 新 (BTCUSDT) |
|------|-------------|-------------|
| 品种 | EUR/USD 现货 | BTCUSDT 永续合约 |
| 周期 | 1H | 1H |
| 数据来源 | 本地 CSV | Binance Futures API |
| 时间范围 | 2020-07 ~ 2025-02 | 2025-10-01 ~ 2026-06-08 |
| 总 K 线数 | — | 5,951 条（去 NaN 后） |
| 数据获取 | 手动 | 自动化 (`fetch_binance_data.py`) |

### 1.2 价格概况

```
BTC 价格范围: $59,362 ~ $125,986
波动率 (小时): 0.50%
ATR(14) 均值:  $585
ATR(14) 范围:  $165 ~ $2,083
```

### 1.3 数据划分

| 划分 | 比例 | K 线数 | 时间范围 |
|------|------|--------|----------|
| 训练集 | 70% | 4,165 | 2025-10-03 ~ 2026-03-13 |
| 测试集 | 30% | 1,786 | 2026-03-13 ~ 2026-06-08 |

---

## 2. 特征工程

### 2.1 技术指标（8 个市场特征）

从 OHLCV 计算，全部为**相对/尺度无关**特征，适合不同价格区间的品种：

| # | 特征 | 含义 | 统计 (训练集) |
|---|------|------|--------------|
| 1 | `rsi_14` | RSI(14) 相对强弱 | μ=48.9, σ=12.3 |
| 2 | `atr_14` | ATR(14) 平均真实波幅 | μ=649.4, σ=243.8 |
| 3 | `ma_20_slope` | MA20 一阶差分 | μ=-11.7, σ=101.9 |
| 4 | `ma_50_slope` | MA50 一阶差分 | μ=-11.3, σ=63.7 |
| 5 | `close_ma20_diff` | 收盘价 - MA20 | μ=-111.3, σ=1135.7 |
| 6 | `close_ma50_diff` | 收盘价 - MA50 | μ=-283.4, σ=1814.3 |
| 7 | `ma_spread` | MA20 - MA50 | μ=-172.1, σ=1104.4 |
| 8 | `ma_spread_slope` | MA 价差变化率 | μ=-0.4, σ=79.9 |

### 2.2 状态特征（3 个）

| # | 特征 | 范围 |
|---|------|------|
| 9 | `position` | -1 (空), 0 (无), +1 (多) |
| 10 | `time_in_trade` | 0 ~ 1 (持仓时间 / 1000) |
| 11 | `unrealized_scaled` | 浮动盈亏 (pip) / 100 |

### 2.3 观测结构

```
obs.shape = (30, 11)
            ↑    ↑
        窗口=30  8 市场特征 + 3 状态特征
```

### 2.4 特征归一化（Z-score）

```
# 从训练集计算 mean/std，对所有 11 个观测通道做 Z-score
# 市场特征 (8): 用训练集真实均值/标准差
# 状态特征 (3): 已缩放，设 mean=0, std=1（恒等变换）
obs_normalized = (obs - feature_mean) / feature_std
```

这是本次训练最关键的修复之一。BTC 原始特征值量级数千（`close_ma20_diff: -6806 ~ +4697`），神经网络期望输入在 [-1, +1] 区间，不做归一化会导致 value_loss 飙到百万级、梯度消失。

---

## 3. 环境参数

### 3.1 交易成本参数

| 参数 | EURUSD (旧) | BTCUSDT (新) | 说明 |
|------|------------|-------------|------|
| pip_value | 0.0001 | 1.0 | 1 pip = $1 价格变动 |
| spread_pips | 1.0 | 0.0 | 合约点差可忽略 |
| commission_pips | 0.0 | 0.0 | 暂未启用 |
| max_slippage_pips | 0.2 | 0.0 | 暂未启用 |
| lot_size | 100,000 | 1.0 | 交易 1 BTC |
| usd_per_pip | $10/pip | $1/pip | = pip_value × lot_size |

### 3.2 pip_value 的完整数据流

```
价格变动(美元) ──÷ pip_value──▶ 点数(pips) ──× usd_per_pip──▶ 盈亏(美元)
                                      │
                               喂给 Agent 做状态特征
```

| 环节 | 公式 | BTCUSDT 示例 |
|------|------|-------------|
| 开仓设 SL/TP | `sl_price = entry ± sl_pips × pip_value` | SL=500 → $500 止损距离 |
| 浮动盈亏 | `unrealized_pips = (price - entry) / pip_value` | $300 价差 → 300 pips |
| 平仓结算 | `equity += net_pips × usd_per_pip` | 300 × $1 = +$300 |

`pip_value` 本质上是一个**缩放因子**，把原始价格波动映射到 Agent 能理解的"点数"空间。在 BTCUSDT 上设为 1.0，因为 BTC 以美元计价，不需要外汇报价精度的换算。

### 3.3 止损/止盈选项

| 版本 | SL 选项 | TP 选项 | 动作数 |
|------|---------|---------|--------|
| 第一版 | 100, 200, 300, 500, 800, 1200, 2000, 3000 (8) | 同左 | 8×8×2+2 = **130** |
| 第二版 | 200, 500, 1000, 2000 (4) | 同左 | 4×4×2+2 = **34** |

```
SL/TP 转换为价格百分比（BTC @ ~$85,000）:
  200 = 0.24%  (0.3x ATR)   — 日内短打
  500 = 0.59%  (0.8x ATR)   — 标准止损
 1000 = 1.18%  (1.5x ATR)   — 波段交易
 2000 = 2.35%  (3.0x ATR)   — 趋势跟随
```

### 3.4 奖励塑形参数

| 参数 | 旧 | 新 | 作用 |
|------|----|----|------|
| hold_reward_weight | 0 | **0.005** | 持仓盈利时给予正向激励（$2.5/bar @ $500 浮盈） |
| unrealized_delta_weight | 0 | **0.1** | 奖励浮动盈亏的改善（密集的每步反馈） |
| open_penalty_pips | 0 | **2.0** | 开仓成本 $2，抑制随机开仓 |
| time_penalty_pips | 0 | **0.02** | 轻微持仓成本 $0.02/bar，避免无限持仓 |

**每步奖励 = realized_pnl + hold_bonus + delta_unrealized - open_cost - time_cost**

---

## 4. 训练历程

### 4.1 第一轮：无归一化 + 无塑形（10K 步快速测试）

| 指标 | 数值 | 诊断 |
|------|------|------|
| value_loss | ~1,000,000 | 🔴 极高 — 特征量纲爆炸 |
| entropy_loss | -4.87 = ln(130) | 🔴 策略完全随机 |
| OOS 收益 | -13.3% | 🔴 稳定亏损 |
| OOS 交易数 | 835 笔 | 🔴 每 2 bar 就开一次仓 |
| OOS 胜率 | 48.5% | 🟡 接近抛硬币 |
| OOS 盈亏比 | 0.82 | 🔴 系统稳定亏损 |
| 止盈:止损 | 57:182 ≈ 1:3.2 | 🔴 SL 频繁被触发 |

**根因分析**: BTC 特征值量级数千（`close_ma20_diff: -6806 ~ +4697`），神经网络期望输入在 [-1, +1] 区间。量纲不匹配 → 输入层饱和 → 梯度死亡 → 学不到任何东西。

### 4.2 第一轮修复：Z-score 归一化（10K 步测试）

```python
# 从训练集计算 mean/std，对环境观测做 Z-score 归一化
market_mean = train_df[feature_cols].mean().values
market_std  = train_df[feature_cols].std().values
feature_mean = concat([market_mean, state_mean])  # 11 维
feature_std  = concat([market_std,  state_std])
```

| 指标 | 归一化前 | 归一化后 | 变化 |
|------|---------|---------|------|
| value_loss | ~1,000,000 | ~750,000 | 仍高但在改善 |
| policy_gradient_loss | -0.002 | **-0.007** | 🟢 策略开始有梯度信号 |
| approx_kl | ~0.00005 | **~0.0007** | 🟢 策略在更新 |
| 训练收益 | +11% | **+155%** | 🟢 |
| 测试收益 | -13% | **+99%** | 🟢 |
| 交易数 | 835 | **223** | 🟢 交易频率合理 |
| 胜率 | 48.5% | **75.8%** | 🟢 |
| 盈亏比 | 0.82 | **1.22** | 🟢 |
| 止盈:止损 | 57:182 | **83:44** | 🟢 止盈多于止损 |

**结论**: 归一化解决了梯度问题，但 10K 步的策略仍接近随机（entropy 仍在最大熵）。

### 4.3 第一轮完整训练：600K 步（归一化开，塑形关，130 动作）

| 指标 | 结果 |
|------|------|
| 最佳 checkpoint | 10K 步, OOS +$15,688 (+57%) |
| 最终模型 OOS | +$433 (几乎不赚) |
| entropy | -4.86 全程不变 = ln(130) |
| approx_kl | 从 1e-4 衰减到 1e-9（策略停止更新） |
| 后段 checkpoints | 多个亏损，整体退化 |

```
OOS 权益随训练的变化:
 10K: $15,688  ← 最好（纯运气撞上趋势）
 30K:  $9,749
100K:  $4,926
300K:  $5,084
600K:    $433  ← 越来越差
```

**结论**: 130 个动作对 4,165 根训练 K 线来说太多。训练越久越差——不是学习，而是噪声主导的随机游走。奖励信号只在平仓时产生（稀疏），Agent 无法在 130 个几乎等价的动作中建立因果关联。

### 4.4 第二轮完整训练：600K 步（归一化 + 塑形 + 34 动作）

| 指标 | 数值 |
|------|------|
| 最佳 checkpoint | 10K 步, OOS **+$20,285 (+103%)** |
| 最终模型 OOS | **+$10,976 (+10%)** |
| 最终模型 IS | **+$38,617 (+286%)** |
| entropy | -3.52 全程不变 = ln(34) |
| 所有 checkpoint OOS 收益 | **全部为正** (最低 $3,396, 最高 $20,285) |

```
OOS 权益随训练的变化:
 10K: $20,285  ← 最佳
 50K: $15,354
100K:  $6,391
200K:  $6,966
300K:  $9,131
400K:  $9,227
500K: $11,444
600K: $10,976  ← 稳定在 $10K 左右
```

---

## 5. 最终结果

```
IS  (训练集) 最终权益:  $38,617  (+286%)
OOS (测试集) 最终权益:  $10,976  (+10%)
Best checkpoint (10K):   $20,285  (+103%)

初始权益: $10,000
```

### 5.1 关键洞察

1. **奖励塑形是核心改进** — `unrealized_delta_weight=0.1` 提供了密集的每步反馈信号。相比第一轮只有平仓时才有奖励，现在 Agent 每步都能感知到"持仓在变好还是变差"，即使在最大熵策略下也能顺着趋势盈利。

2. **动作空间缩小有效但不彻底** — 34 个动作比 130 好很多，OOS 不再出现亏损。但 entropy 仍停在最大熵（-3.52 = ln 34），说明策略网络从未真正学会区分不同 SL/TP 组合的优势。34 动作 × 4165 K 线 = 每个动作平均仅 ~122 次出现。

3. **BTC 上涨趋势有贡献** — 2025-10 到 2026-06，BTC 从 ~$60K 涨到 ~$126K（翻倍）。在随机多空分配下，多头自然胜率高于空头。测试集的 +10% 收益中有一部分来自这个市场结构。

4. **IS/OOS 存在差距** — 训练集 +286% vs 测试集 +10%。早期 checkpoint (10K) OOS 最好，后期模型倾向于记住训练集模式，泛化能力下降。这提示了过拟合风险。

5. **策略网络实际未收敛** — 两轮训练中 entropy 从未低于理论最大值，说明 PPO 的 MLP policy 在这个任务上未能学到有效的动作偏好。收益主要来自环境设计（奖励塑形 + 趋势 + 合理的 SL/TP），而非 RL 策略本身。

---

## 6. 改进方向

| 方向 | 优先级 | 措施 |
|------|--------|------|
| 增加训练数据 | ⭐⭐⭐ | 引入更多历史数据或多品种数据，使每个动作有足够的训练样本 |
| 进一步缩小动作空间 | ⭐⭐⭐ | 3×3 SL/TP = 18 OPEN，总计 20 个动作 |
| 降低学习率或调整 PPO 超参 | ⭐⭐ | 当前 lr=0.0003 可能偏大，导致后期退化 |
| 换用 SAC/QR-DQN | ⭐⭐ | Off-policy 算法在处理离散大动作空间时样本效率更高 |
| 引入 Market Regime 特征 | ⭐⭐ | 波动率分位数、趋势强度等，让策略感知"该用什么模式交易" |
| 多品种训练 | ⭐ | 同时训练 ETH、SOL 等，增强泛化能力 |
| 启用交易成本 | ⭐ | spread + commission 模拟真实 Binance 手续费 |

---

## 7. 文件清单

| 文件 | 作用 |
|------|------|
| `fetch_binance_data.py` | 从 Binance Futures API 拉取 BTCUSDT 1H K 线数据 |
| `data/BTCUSDT_Perpetual_1H_2025-10-01_2026-06-08.csv` | 原始 K 线数据 (6001 条) |
| `indicators.py` | 特征工程：RSI, ATR, MA 衍生指标 (8 个市场特征) |
| `trading_env.py` | Gym 环境：ForexTradingEnv (观测/动作/奖励/风控) |
| `train_agent.py` | PPO 训练主程序 (数据加载/归一化/训练/评估) |
| `test_agent.py` | 模型评估 & 交易记录导出 |
| `resume_training.py` | 从 checkpoint 继续训练 |
| `analyze_trades.py` | 交易记录分析脚本 |
| `model_eurusd_best.zip` | 最佳模型权重 (10K checkpoint) |
| `equity_curves.png` | IS vs OOS 权益曲线对比图 |
| `trade_history_output.csv` | 测试集交易明细 |
| `checkpoints/` | 训练检查点 (每 10K 步) |

---

## 8. 运行命令

```bash
# 1. 拉取数据
python fetch_binance_data.py

# 2. 完整训练 (600K 步, ~15 分钟)
python train_agent.py

# 3. 评估最佳模型
python test_agent.py

# 4. 从 checkpoint 继续训练
python resume_training.py

# 5. 分析交易记录
python analyze_trades.py
```
